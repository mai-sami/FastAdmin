const api_token = JSON.parse(localStorage.getItem("token"));

// console.log(api_token, "api_token");
export const config = {
  email: "mai@gmail.com",
  password: "mai123",
  api_token: api_token,
};
